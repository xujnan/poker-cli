# 洗牌由可信服务端完成，不做可验证洗牌，但随机源必须可播种

服务端持有整副牌，只把每个玩家自己的底牌发给他，因此服务端在密码学意义上是完全可信的——跑服务端的人理论上能看到所有底牌。我们不做 commit-reveal 式的可验证洗牌。

理由是威胁模型不成立：同机对战场景下跑服务端的就是玩家本人。但随机源必须支持 `--seed` 显式播种，因为没有可复现的牌序就无法给边池分配这类最易出错的逻辑写回归测试，agent 评测时也需要它来控制变量。
