# 服务端显式启动，并且是牌桌状态的唯一权威

牌桌状态活在一个显式启动的 `poker serve` 进程里，客户端通过 Unix domain socket 连接它。不做「首次建桌时自动 fork 后台 daemon」，也不做「无服务端、多客户端共享文件加锁」。

显式服务端让「谁持有真相」在架构上可见，也让服务端日志在人机混战出问题时直接可读；隐式 daemon 的便利换来的是日志去向不明和僵尸进程，共享文件方案则把并发正确性全部压到客户端。便利性后续可以用 `--detach` 补回来。
