# 先只支持同机传输，但把传输收敛到接口后面

多个终端要共享同一张牌桌，传输层有同机（Unix domain socket）与跨机（TCP）两条路。第一版只落地同机传输，并把它收敛到一个传输接口后面，以便后续加联机时换实现而不动牌局逻辑。

跨机会立刻把部署位置、地址发现、NAT 与断线重连拖进来，而这些与本项目真正要打磨的牌局状态机无关；同机方案零端口、零防火墙，且已经足够覆盖「多终端对战」和「AI agent 对战环境」这两个目标场景。
