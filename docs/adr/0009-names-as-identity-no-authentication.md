# 玩家身份就是名字，不做任何认证

`poker join ABC123 --as alice` 中的名字即身份。重连就是用同一个名字再次 join，服务端按名字认回原座位与 Stack；断线的玩家视同 Sitting Out，筹码不没收。不发 token、不存凭据。

这意味着任何人都能冒充 alice 接管她的筹码——在同机场景下这个威胁模型不成立，能运行客户端的就是本机用户本人。为一个不存在的攻击加一层凭据存储，是在给未来的联机功能提前付成本；等 TCP 传输真的进来时，认证会和它一起进来。
