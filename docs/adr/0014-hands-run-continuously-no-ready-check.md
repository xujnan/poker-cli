# 手牌自动连续进行，不设准备确认

牌桌上凑够两个有筹码的玩家即自动开始第一手，此后每手结束自动开始下一手，中间隔 `--hand-delay`（默认 3s，自对弈时设 0）。没有 `ready` 命令，也不要求任何人逐手确认。玩家需要喘息时用 `sitout` 主动进入 Sitting Out，`sitin` 回来。

逐手确认会让 AI agent 的无人值守自对弈彻底跑不动——几千手牌不可能每手都等一次握手。而人类那一侧的真实需求其实是「我想歇会儿」，而不是「我要为每一手牌点头」，这个需求由已有的 Sitting Out 状态覆盖，不需要引入新概念。
