# 牌桌状态是易失的，手牌历史是只追加的

`poker serve` 的牌桌状态纯内存，进程退出即蒸发，不做落盘与恢复。但每手牌结束时，一条含随机种子与全部 Hole Cards 的完整记录会追加写入 Hand History 文件。

这两者分工明确，不要混淆：历史是只追加的事实日志，服务于 agent 的训练评测数据和 bug 复现；状态是易失的运行时。让历史去承担恢复状态的职责，会立刻把状态迁移与版本兼容拖进来，而同机开发场景下服务端挂掉重开是完全可接受的。
